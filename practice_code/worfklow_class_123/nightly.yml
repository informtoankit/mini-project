name: Nightly Workflow

on:
  schedule:
    - cron: "0 18 * * 1-5"

  workflow_dispatch:

jobs:
  nightly:

    runs-on: ubuntu-latest

    steps:
      - name: Nightly Check
        run: |
          echo "Nightly Run"
          date -u